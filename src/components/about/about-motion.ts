/** Shared cinematic fade timing for About slideshow */
export const FADE_EASE = [0.4, 0, 0.2, 1] as const;

/** Opacity crossfade between slides (seconds) */
export const CROSSFADE_DURATION_S = 2;

/** Time each slide is held before the crossfade starts (seconds) */
export const SLIDE_HOLD_S = 2.5;
